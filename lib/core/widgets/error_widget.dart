import 'package:flutter/material.dart';

class ErrorToGetWidget extends StatelessWidget {
  const ErrorToGetWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Something error"));;
  }
}
